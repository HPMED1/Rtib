[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rtib"
version = "0.1.0"
description = "Local-first AI sorter/formatter for messy unstructured text"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Rtib dev", email = "nerohpmed@gmail.com" }]
keywords = ["llm", "ollama", "structured-output", "data-cleaning", "etl"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: X11 Applications :: Qt",
    "Environment :: Win32 (MS Windows)",
    "Intended Audience :: End Users/Desktop",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Text Processing",
]
dependencies = [
    "PySide6>=6.7",
    "httpx>=0.27",
    "pydantic>=2.7",
    "typer>=0.12",
    "openpyxl>=3.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pyinstaller>=6.6",
    "ruff>=0.5",
    "Pillow>=10",  # only for scripts/generate-icon.py
]

[project.scripts]
rtib = "rtib.cli:main"

[project.gui-scripts]
rtib-gui = "rtib.cli:gui_main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
rtib = ["resources/icons/*", "resources/styles/*"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
# E501: line length is enforced via formatter, not lint.
# B008: typer.Option(...) in defaults is the idiomatic typer pattern.
ignore = ["E501", "B008"]
